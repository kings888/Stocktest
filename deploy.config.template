# 部署配置
GITHUB_REPO=           # GitHub仓库名，例如：yourusername/stock-monitor
SERVER_HOST=           # 服务器IP，例如：15.164.136.151
SERVER_USERNAME=       # 服务器用户名
DEPLOY_PATH=          # 部署路径，默认为：/home/{USERNAME}/StockTest
BRANCH=main           # 部署分支，默认为main 